{
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "g:build": "cd $INIT_CWD && tsc -b tsconfig.json",
    "test": "yarn workspaces foreach run test"
  },
  "devDependencies": {
    "@types/prettier": "^2.2.3",
    "prettier": "^2.2.1",
    "typescript": "^4.2.4"
  }
}
